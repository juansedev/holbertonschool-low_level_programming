#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	char s[] = "holberton!";
	char a[] = "a";
	char b[] = "Vagrant pro1234567890123456789vides a command-line tool for loading and managing virtual operating systems. We will be using the VirtualBox provider in this tutorial.Th guide will walk you through installing Vagrant on CentOS 7.how to install vagrant on centos7rerequisitesA user account with sudo privileges on CentOS 7 systemAccess to a command line/terminal (Menu > Applications > Utilities > TerminalThe yum package manager, included by defaultInstall Vagrant on CentOS tep Refresh Software RepositoriIn a";	
	char c[] = "as";


	printf("%s\n", s);
	rev_string(s);
	printf("%s\n", s);


	printf("%s\n", a);
	rev_string(a);
	printf("%s\n", a);



	printf("%s\n", b);
	rev_string(b);
	printf("%s\n", b);



	printf("%s\n", c);
	rev_string(c);
	printf("%s\n", c);
	return (0);
}
