#include <stdlib.h>
#include <time.h>
/*this code generates a random number  a validate*/
/*if this number is positive, negative o zero*/

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	printf("%d\n", n);
}
