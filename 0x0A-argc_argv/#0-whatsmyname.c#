a#include "holberton.h"
/**
  * main - Program that prints its name
  * @argc: count
  * @argv: array
  *
  * Return: Always 0
  **/
int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
		_putchar(*((argv[0]) + i));
	}
	_putchar('\n');
	return (0);
}
