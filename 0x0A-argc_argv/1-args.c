#include "holberton.h"
#include <stdio.h>
/**
  * main - Program that prints the number of arguments passed into it.
  * @argc: count
  * @argv: array
  *
  * Return: Always 0
  **/
int main(int argc, char *argv[])
{
	(void) argv;

	printf("%d \n", argc);

	return (0);
}
