# the compiler: gcc for C program, define as g++ for C++
CC = gcc

# the build target executable
TARGET = holberton

all: $(TARGET)

$(TARGET): $(TARGET).c
	$(CC) main.c $(TARGET).c -o $(TARGET)

clean:
	$(RM) $(TARGET)

